{
  "name": "Cosmic Compass Dev",
  "image": "mcr.microsoft.com/devcontainers/typescript-node:20",
  "workspaceFolder": "/workspace",
  "remoteUser": "node",
  "updateContentCommand": "bash -lc 'if [ -f pnpm-lock.yaml ]; then corepack enable && pnpm i; elif [ -f yarn.lock ]; then corepack enable && yarn install --frozen-lockfile; elif [ -f package-lock.json ]; then npm ci; else npm i; fi'",
  "postStartCommand": "bash -lc 'if [ -f package.json ]; then if jq -e .scripts.dev package.json >/dev/null 2>&1; then npm run dev -- --host 0.0.0.0; elif jq -e .scripts.start package.json >/dev/null 2>&1; then npm start; else echo \"No dev/start script found\"; fi; fi'",
  "forwardPorts": [
    3000,
    9002
  ],
  "portsAttributes": {
    "3000": {
      "label": "Next.js Dev Server",
      "onAutoForward": "openBrowser"
    },
    "9002": {
      "label": "Next.js Dev Server (Alt)",
      "onAutoForward": "openBrowser"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "bradlc.vscode-tailwindcss",
        "ms-vscode.vscode-typescript-next",
        "GitHub.copilot",
        "GitHub.copilot-chat"
      ]
    }
  },
  "containerEnv": {
    "NODE_ENV": "development"
  }
}